# Playground

A collection of random little projects, experiments, and tests — primarily built with Claude Code using HTML, CSS, and JavaScript.

## Structure

Each project lives in its own branch. Branches are generally standalone and not merged into main.

## Stack

- HTML / CSS / JavaScript (primary)
- Built with Claude Code
